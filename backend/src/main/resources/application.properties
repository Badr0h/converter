# ====== APPLICATION CONFIGURATION ======
spring.application.name=converter-backend
server.port=${SERVER_PORT:8081}
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# ====== DATABASE CONFIGURATION ======
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/unit_conversion?useSSL=false&serverTimezone=UTC}
spring.datasource.username=${DATABASE_USERNAME:converter_admin}
spring.datasource.password=${DATABASE_PASSWORD:converter112233}
spring.datasource.driver-class-name=org.postgresql.Driver

# ====== JPA / HIBERNATE CONFIGURATION ======
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ====== FLYWAY CONFIGURATION ======
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# ====== ACTUATOR CONFIGURATION ======
# Enable health endpoints
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized
management.endpoint.health.show-components=always

# Health check settings
management.health.db.enabled=true
management.health.datasources.enabled=true
management.health.ping.enabled=true

# Security for actuator endpoints
management.endpoints.web.access.default=deny
management.endpoint.health.access=read_only

# ====== LOGGING CONFIGURATION ======
# Development profile - verbose logging for debugging
logging.level.org.springframework=INFO
logging.level.com.converter=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.springdoc=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Console logging pattern for development
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# ====== JWT CONFIGURATION ======
jwt.secret=${JWT_SECRET:SGVsbG9Xb3JsZFNlY3JldEtleUZvckpXVFRva2VuU2lnbmluZw==}
jwt.expiration=${JWT_EXPIRATION:86400000}

# ====== EMAIL CONFIGURATION ======
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:oussaihbadreddine@gmail.com}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=${MAIL_HOST:smtp.gmail.com}

# ====== FRONTEND URL CONFIGURATION ======
frontend.url=${FRONTEND_URL:http://localhost:4200}
frontend.cors.allowed-origins=${FRONTEND_CORS_ALLOWED_ORIGINS:http://localhost:4200,http://localhost:8081}

# ====== PAYPAL CONFIGURATION ======
paypal.client.id=${PAYPAL_CLIENT_ID}
paypal.client.secret=${PAYPAL_CLIENT_SECRET}
paypal.mode=${PAYPAL_MODE:sandbox}
paypal.return.url=${PAYPAL_RETURN_URL:http://localhost:4200/payment-success}
paypal.cancel.url=${PAYPAL_CANCEL_URL:http://localhost:4200/payment-cancel}

# ====== STRIPE CONFIGURATION ======
stripe.api.key=${STRIPE_API_KEY}
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET}

# ====== OPENAI CONFIGURATION ======
openai.api.url=${OPENAI_API_URL:https://api.openai.com/v1/chat/completions}
openai.api.key=${OPENAI_API_KEY}

# ====== PAYMENT SETTINGS ======
payment.currency.default=${PAYMENT_CURRENCY:USD}
payment.timeout.minutes=${PAYMENT_TIMEOUT:15}
payment.max.retries=${PAYMENT_MAX_RETRIES:3}
payment.cleanup.enabled=${PAYMENT_CLEANUP_ENABLED:true}
payment.cleanup.cron=${PAYMENT_CLEANUP_CRON:0 0 2 * * ?}
