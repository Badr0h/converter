import { Routes } from '@angular/router';
import { ConverterComponent } from './converter/converter.component';

export const CONVERSION_ROUTES: Routes = [
  {
    path: '',
    component: ConverterComponent
  }
];